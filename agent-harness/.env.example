# Required by both the worker agent and the supervisor decision calls
ANTHROPIC_API_KEY=sk-ant-...

# Optional: model used for the supervisor's allow/deny decisions
SUPERVISOR_MODEL=claude-sonnet-4-6
